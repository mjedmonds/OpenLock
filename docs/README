Requires:

OpenLock and all of its dependencies

Sphinx

Create the RST output from the dogstring-ed .py files with:

`sphinx-apidoc -o source/ ../` (from inside the docs directory).


RST files are written to docs/source/


If rebuilding the RST output, first `rm source/*`.

Create HTML files with `make html` (from inside the docs directory).

Index of the HTML output is located at docs/_build/html/index.html
